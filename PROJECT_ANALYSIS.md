# 宿舍管理系统详细解读文档

## 📋 项目概述

**项目名称**: Rwb-dormitory-management-system  
**项目类型**: Java桌面应用程序  
**开发语言**: Java 11  
**技术栈**: JavaFX + MySQL + Maven  
**作者**: 任万博 (2413041837)  
**开发时间**: 2026年1月  

### 🎯 项目定位
这是一个基于Java + JavaFX开发的现代化宿舍管理系统，采用MVC架构设计，实现了宿舍资源管理、学生住宿管理、缴费违纪管理、查询统计及维修申请管理等功能。系统支持管理员和学生两种用户角色，提供了完整的宿舍日常管理解决方案。

## 🏗️ 技术架构

### 核心技术栈
- **前端界面**: JavaFX 17.0.2 + FXML + CSS
- **后端语言**: Java 11
- **数据库**: MySQL 8.0+
- **构建工具**: Maven 3.6+
- **日志框架**: SLF4J + Logback
- **图表组件**: JFreeChart 1.5.3

### 系统架构模式
系统采用经典的**MVC (Model-View-Controller)** 设计模式：

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   View层        │    │ Controller层    │    │  Model层        │
│ (JavaFX界面)    │◄──►│ (业务逻辑处理)  │◄──►│ (数据模型)      │
│ - FXML界面      │    │ - 用户请求处理  │    │ - 实体类        │
│ - 视图控制器    │    │ - 数据校验      │    │ - DAO接口       │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 📁 项目结构详解

### 源码目录结构
```
src/main/java/org/dorm/
├── controller/          # 控制器层（业务逻辑）
│   ├── AccommodationController.java
│   ├── AnnouncementController.java
│   ├── BedController.java
│   ├── DormitoryController.java
│   ├── LoginController.java
│   ├── PaymentController.java
│   ├── ReminderController.java
│   ├── RepairApplicationController.java
│   ├── StudentController.java
│   ├── UserController.java
│   └── ViolationController.java
├── model/               # 模型层
│   ├── dao/            # 数据访问层接口
│   ├── entity/         # 实体类
│   │   ├── Accommodation.java
│   │   ├── Announcement.java
│   │   ├── Bed.java
│   │   ├── Dormitory.java
│   │   ├── Payment.java
│   │   ├── Reminder.java
│   │   ├── RepairApplication.java
│   │   ├── Student.java
│   │   ├── User.java
│   │   └── Violation.java
│   └── service/        # 业务逻辑服务层
├── util/               # 工具类
│   ├── DatabaseUtil.java
│   └── DataUpdateManager.java
└── view/               # 视图层（JavaFX控制器）
    ├── AccommodationManagementController.java
    ├── AdminRepairController.java
    ├── ChangePasswordController.java
    ├── DormitoryManagementController.java
    ├── LoginViewController.java
    ├── MainController.java
    ├── PaymentManagementController.java
    ├── RegisterViewController.java
    ├── ReminderManagementController.java
    ├── StatisticsController.java
    ├── StudentDashboardController.java
    ├── StudentInfoController.java
    ├── StudentMainController.java
    ├── StudentManagementController.java
    ├── StudentRepairController.java
    ├── SubmitRepairController.java
    ├── ViolationManagementController.java
```

### 资源文件结构
```
src/main/resources/
├── fxml/               # JavaFX界面文件
│   ├── login.fxml
│   ├── main.fxml
│   ├── student_management.fxml
│   ├── dormitory_management.fxml
│   ├── accommodation_management.fxml
│   ├── payment_management.fxml
│   ├── violation_management.fxml
│   ├── repair_application.fxml
│   └── statistics.fxml
├── css/                # 样式文件
│   └── application.css
└── images/             # 图片资源
```

## 🗄️ 数据库设计详解

### E-R关系图
```
┌─────────┐     ┌─────────┐
│   用户  │─────│   学生  │
│ (User)  │1:1  │(Student)│
└─────────┘     └─────────┘
                    │
                    │1:*
                    ▼
┌─────────┐     ┌─────────┐     ┌─────────┐
│ 缴费记录 │     │ 违纪记录 │     │ 维修申请 │
│(Payment)│     │(Violation│     │(RepairApp│
└─────────┘     └─────────┘     └─────────┘
                    ▲
                    │1:*
               ┌─────────┐
               │   床位  │
               │  (Bed)  │
               └─────────┘
                    ▲
                    │1:*
               ┌─────────┐
               │   宿舍  │
               │(Dormitory)
               └─────────┘
```

### 核心数据表详解

#### 1. 用户表 (user)
```sql
CREATE TABLE user (
    user_id VARCHAR(20) PRIMARY KEY,      -- 用户名，唯一标识
    password VARCHAR(50) NOT NULL,        -- 登录密码
    user_type ENUM('管理员','学生') NOT NULL -- 用户类型
);
```
**说明**: 系统基础用户表，支持管理员和学生两种角色。

#### 2. 学生信息表 (student)
```sql
CREATE TABLE student (
    student_id VARCHAR(20) PRIMARY KEY,   -- 学号，主键
    password VARCHAR(50) NOT NULL,        -- 登录密码
    name VARCHAR(50) NOT NULL,            -- 姓名
    college_name VARCHAR(50) NOT NULL,    -- 学院名称
    class_name VARCHAR(50) NOT NULL,      -- 班级名称
    gender CHAR(1) NOT NULL,              -- 性别：男/女
    birthday DATE,                        -- 出生日期
    email VARCHAR(100),                   -- 电子邮箱
    phone VARCHAR(20),                    -- 联系电话
    bed_id VARCHAR(20),                   -- 关联床位号，外键
    FOREIGN KEY (bed_id) REFERENCES bed(bed_id)
);
```

#### 3. 宿舍信息表 (dormitory)
```sql
CREATE TABLE dormitory (
    dorm_id VARCHAR(20) PRIMARY KEY,      -- 宿舍号
    building VARCHAR(50) NOT NULL,        -- 楼栋名称
    floor INT NOT NULL,                   -- 楼层
    capacity INT NOT NULL,                -- 可容纳人数
    manager_name VARCHAR(50),             -- 管理员姓名
    manager_phone VARCHAR(20)             -- 管理员电话
);
```

#### 4. 床位信息表 (bed)
```sql
CREATE TABLE bed (
    bed_id VARCHAR(20) PRIMARY KEY,       -- 床位号
    dorm_id VARCHAR(20) NOT NULL,         -- 宿舍号，外键
    status ENUM('空闲','占用') DEFAULT '空闲', -- 床位状态
    student_id VARCHAR(20),               -- 占用学生学号，外键
    FOREIGN KEY (dorm_id) REFERENCES dormitory(dorm_id),
    FOREIGN KEY (student_id) REFERENCES student(student_id)
);
```

#### 5. 维修申请表 (repair_application) - ⭐ 新增功能
```sql
CREATE TABLE repair_application (
    apply_id VARCHAR(50) PRIMARY KEY,     -- 申请单号
    student_id VARCHAR(20) NOT NULL,      -- 申请人学号
    fault_location VARCHAR(100) NOT NULL, -- 故障位置
    fault_desc TEXT NOT NULL,             -- 故障描述
    contact_phone VARCHAR(20) NOT NULL,   -- 联系电话
    apply_time DATETIME NOT NULL,         -- 申请时间
    status VARCHAR(20) DEFAULT 'pending', -- 处理状态
    handler VARCHAR(50),                  -- 处理人
    finish_time DATETIME,                 -- 完成时间
    FOREIGN KEY (student_id) REFERENCES student(student_id)
);
```

## 🎯 功能模块详解

### 1. 用户认证模块
- **登录功能**: 支持管理员和学生双角色登录
- **密码管理**: 支持密码修改和重置
- **权限控制**: 基于角色的功能访问控制

### 2. 宿舍资源管理
- **宿舍管理**: 宿舍信息的增删改查
- **床位管理**: 床位分配和状态管理
- **住宿管理**: 学生入住和退宿管理

### 3. 学生信息管理
- **学生档案**: 学生基本信息管理
- **住宿安排**: 学生的宿舍分配
- **信息统计**: 学生数据统计分析

### 4. 缴费管理
- **缴费记录**: 学生的住宿费缴费管理
- **缴费状态**: 缴费状态跟踪
- **历史查询**: 缴费历史记录查询

### 5. 违纪管理
- **违纪记录**: 学生违纪行为的记录
- **处罚管理**: 违纪处罚的登记和管理
- **历史追溯**: 违纪历史查询

### 6. 维修申请管理 ⭐ (核心新增功能)
- **学生端功能**:
  - 在线提交维修申请
  - 填写详细故障信息
  - 查看申请处理进度
  - 历史申请记录查询

- **管理员端功能**:
  - 查看所有待处理申请
  - 申请状态更新管理
  - 处理进度跟踪
  - 维修完成确认

### 7. 统计分析模块
- **数据可视化**: 使用图表展示各类统计信息
- **多维度分析**: 按学院、宿舍等维度统计
- **趋势分析**: 数据变化趋势展示

## 💻 关键技术实现

### 数据库连接管理
```java
public class DatabaseUtil {
    // 数据库连接参数配置
    private static final String URL = "jdbc:mysql://localhost:3306/dormitory_management?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true";
    private static final String USER = "root";
    private static final String PASSWORD = "123456";

    // 静态初始化块，项目启动时加载驱动
    static {
        Class.forName(DRIVER);
    }

    // 获取连接
    public static Connection getConnection() throws SQLException {
        return DriverManager.getConnection(URL, USER, PASSWORD);
    }
}
```

### 维修申请业务逻辑
```java
public class RepairApplicationServiceImpl implements RepairApplicationService {
    @Override
    public boolean submitRepairApplication(RepairApplication application) {
        // 1. 参数校验
        // 2. 生成申请单号
        // 3. 设置申请时间和初始状态
        // 4. 持久化到数据库
        // 5. 日志记录
    }

    @Override
    public String generateApplyId(String studentId) {
        // 生成规则：WX + 年月日 + 学生学号后4位 + 随机数
        // 例如：WX202601142014A1B2
    }
}
```

### JavaFX界面控制
```java
public class MainController implements DataUpdateManager.DataUpdateListener {
    @FXML
    private void handleStudentManagement(ActionEvent event) {
        // 1. 加载学生管理界面
        // 2. 设置界面数据
        // 3. 显示界面
    }

    @FXML
    private void handleRepairManagement(ActionEvent event) {
        // 维修申请管理逻辑
    }
}
```

## 🚀 开发环境配置

### 系统要求
- **操作系统**: Windows 10/11, macOS, Linux
- **Java版本**: JDK 11 或更高版本
- **数据库**: MySQL 8.0+
- **构建工具**: Maven 3.6+

### 环境搭建步骤
1. **安装JDK 11**
2. **安装MySQL 8.0**
3. **创建数据库**:
   ```sql
   CREATE DATABASE dormitory_management CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
4. **配置数据库连接** (DatabaseUtil.java)
5. **编译运行**:
   ```bash
   mvn clean compile
   mvn javafx:run
   ```

### 测试账号
- **管理员**: admin / 123456
- **学生**: 2024001 / 123456 (张三)

## 🌟 特色功能亮点

### 1. 维修申请管理系统 ⭐
- **创新点**: 新增的在线维修申请功能
- **用户体验**: 学生可随时提交维修申请，管理员可实时处理
- **状态跟踪**: 四状态流程：待受理 → 已受理 → 维修中 → 已完成
- **智能单号**: 自动生成唯一申请单号，便于管理

### 2. 现代化UI设计
- **JavaFX界面**: 采用现代化的JavaFX界面框架
- **响应式布局**: 支持窗口大小调整
- **美观样式**: 自定义CSS样式，提升用户体验

### 3. 完善的权限管理
- **角色分离**: 管理员和学生功能完全分离
- **界面适配**: 根据用户角色显示不同功能模块
- **数据隔离**: 学生只能查看自己的相关数据

### 4. 数据可视化
- **统计图表**: 使用JFreeChart生成各类统计图表
- **实时更新**: 数据变化时图表自动更新
- **多维度分析**: 支持按不同维度进行数据分析

### 5. 完善的日志系统
- **SLF4J日志**: 使用专业日志框架
- **分级记录**: 支持DEBUG、INFO、WARN、ERROR级别
- **操作追踪**: 记录重要业务操作

## 📈 项目优势

### 技术优势
1. **架构清晰**: 采用标准MVC模式，代码结构清晰
2. **技术成熟**: 使用主流的Java技术栈
3. **扩展性好**: 模块化设计，便于功能扩展
4. **维护方便**: 完善的文档和注释

### 功能优势
1. **功能完整**: 涵盖宿舍管理的所有核心功能
2. **用户友好**: 界面直观，操作简单
3. **数据准确**: 完善的数据校验和业务逻辑
4. **性能优良**: 优化的数据库设计和查询

### 创新亮点
1. **维修申请**: 新增在线维修申请功能
2. **状态管理**: 完善的申请状态流转
3. **数据可视化**: 丰富的统计图表展示
4. **实时提醒**: 智能提醒功能

## 🔧 部署和运行

### 本地开发运行
```bash
# 1. 克隆项目
git clone <repository-url>

# 2. 进入项目目录
cd Rwb-dormitory-management-system

# 3. 编译项目
mvn clean compile

# 4. 运行应用程序
mvn javafx:run
```

### 生产环境部署
1. **打包应用程序**:
   ```bash
   mvn clean package
   ```

2. **数据库迁移**: 执行数据库初始化脚本

3. **配置文件**: 修改生产环境数据库配置

4. **启动应用**: 使用JavaFX运行时启动

## 📝 总结

这个宿舍管理系统是一个功能完整、架构合理、技术先进的Java桌面应用程序。项目采用了现代化的技术栈，实现了宿舍管理的核心功能，并在此基础上新增了创新的维修申请管理功能。代码结构清晰，文档完善，是一个优秀的课程设计作品。

**核心价值**:
- ✅ 技术实现成熟稳定
- ✅ 功能设计实用全面
- ✅ 用户体验良好
- ✅ 代码质量高
- ✅ 文档完善详细

**作者信息**: 任万博 (2413041837)  
**完成时间**: 2026年1月  
**项目类型**: 课程设计作品